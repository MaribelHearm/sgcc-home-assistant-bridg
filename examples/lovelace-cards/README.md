# Lovelace 卡片示例

把 YAML 里的 `4840` 替换成自己的户号后四位。

- `01-project-original.yaml`：项目原始卡片，尽量只用 HA 内置卡片。
- `02-legacy-electricity-ui.yaml`：消逝的电力 UI 风格，需 `apexcharts-card` / `card-mod`。
- `03-nord-glass.yaml`：Nord 毛玻璃版，需同时放置 `../custom-cards/sgcc-electricity-card.js`。
