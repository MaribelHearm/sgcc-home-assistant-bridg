# Source Notes: Add-on 接入官方 Chrome browser-service

- 任务以本仓库当前运行入口和 Docker/Add-on 配置为准。
- Home Assistant Add-on `config.yaml` 的 `image`/`version`/`schema list(...)` 用法已按官方开发文档核对。
- 运行时验证以本机 Docker build、Compose config、Add-on embedded smoke、sidecar smoke、app→sidecar CDP attach smoke 为准。
